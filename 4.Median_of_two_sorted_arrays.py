class Solution:
    def findMedianSortedArrays(self, nums1: list[int], nums2: list[int]) -> float:
        m,n = len(nums1),len(nums2)

    def findAvg(self, arr):
        l = len(arr)
        if l % 2 == 0:
            return arr[l/2-1]
        else:
            return 

solObj = Solution()
print (solObj.findMedianSortedArrays([1,3],[2]))